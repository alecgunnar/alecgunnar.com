pipeline {
  agent any

  stages {
    stage('Checkout') {
      steps {
        echo 'Git Checkout'
        sh('ls')
      }
    }

    stage('Deploy') {
      steps {
        echo 'Testing...1...2...3...4'
        sh('ls')
      }
    }
  }
}
